 Faça um programa que peça para o usuário digitar um número qualquer porém só termine quando o usuário digitar o número 4;

#include <stdio.h>

int main() {
    int numero;
    
    for(numero = 0; numero != 4; ) {
        printf("Digite um número: ");
        scanf("%d", &numero);
    }
    printf( "TERMINADO", numero);
   
    return 0;
}
